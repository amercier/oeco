<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    'application' %>
    <%= javascript_include_tag 'vendor/modernizr' %>
    <%= csrf_meta_tags %>
  </head>

  <body>

    <header class="row">
      <div class="logo small-8 small-offset-2 columns">
        <%= image_tag 'oeco.jpg', :alt => 'oeco architecte' %>
      </div>
      <nav class="nav-main small-12 columns">
        <ul class="row no-bullet">
          <li class="small-12 columns small-text-center"><%= link_to 'projets', news_index_path =%></li>
          <li class="small-12 columns small-text-center"><%= link_to 'agence', news_index_path =%></li>
        </ul>
      </nav>
    </header>

    <%= yield %>
    <%= javascript_include_tag 'application' %>
  </body>
</html>
